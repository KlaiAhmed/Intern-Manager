import { AuthScreen } from '../components/AuthScreen'

/**
 * Page dediee a la route /login avec le formulaire d'authentification.
 */
export function LoginPage() {
  return <AuthScreen variant="login" />
}
